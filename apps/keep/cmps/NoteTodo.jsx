
export function NoteTodo({note}) {
    return <section>

        <h1>Todo note</h1>
        <h2>{note.info.label}</h2>

        {/* {cars.map(car => <CarPreview car={car} key={car.id} />)} */}
    </section>
}